function cloak() {
  var title = document.getElementById("cloakTabText").value;
  var url = window.location;
  var favicon = document.getElementById("cloakTabText-2").value;
  cloakTab(title, url, favicon);
}
